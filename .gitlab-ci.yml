stages:
  - install
  - test
  - deploy

install_stage:
  stage: install
  script:
    - node -v
    - yarn -v
    - yarn

test_stage:
  stage: test
  script:
    - yarn test

deploy_stage:
  stage: deploy
  script:
    - chmod +x ci/deploy_stage.sh
    - ci/deploy_stage.sh
  only:
    - main
